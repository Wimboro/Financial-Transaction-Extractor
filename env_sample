# Gmail to Sheets Configuration

# Google API credentials
# Keep credentials.json in the same directory as the script

# Gemini API Key (required)
GEMINI_API_KEY=your_gemini_api_key_here

# Google Sheet ID (required)
SPREADSHEET_ID=your_sheet_id_here

# User ID for email processor (optional)
PROCESSOR_USER_ID=email-processor

# Gmail accounts to process (comma-separated list)
# Each account will use a separate token file: token_account1.json, token_account2.json, etc.
# Example: GMAIL_ACCOUNTS=account1,account2,work
GMAIL_ACCOUNTS=wgppra,wimborogp,wimboro,fara

# Gmail search query (optional)
# Default: subject:(Transfer OR Pembayaran OR Transaksi OR payment OR transaction) is:unread newer_than:1d
# GMAIL_SEARCH_QUERY=your_custom_search_query

# Telegram Notification Settings
# Set TELEGRAM_ENABLED to true to enable notifications
TELEGRAM_ENABLED=false
# Your Telegram bot token (from BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
# Multiple chat IDs where notifications should be sent (comma-separated)
# Can include user IDs, group IDs, or channel IDs
TELEGRAM_CHAT_IDS=id1,id2,id3
# For backward compatibility (single chat ID) - TELEGRAM_CHAT_IDS is preferred
# TELEGRAM_CHAT_ID=your_telegram_chat_id 